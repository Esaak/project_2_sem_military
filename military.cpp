//
// Created by п on 14.02.2022.
//

#include "military.h"
military::military(int x, int y, int size,int firepower, int hp, int distance, char direction):coordinates(x, y), size(size), firepower(firepower), hp(hp), distance(distance), direction(direction) {}
